[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "xuanling-dcp-kernel-candidate"
version = "0.1.0"
description = "Candidate executable DCP dependency and continuity kernel"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["dcp_kernel*"]
