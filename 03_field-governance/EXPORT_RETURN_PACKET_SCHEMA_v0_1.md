# Receiver-Owned Export / Return Lineage Specimen

**Lifecycle:** SUCCESSOR_COVERED / LINEAGE_ONLY  
**Current eligibility as fixed packet schema:** false  
**Reclaim disposition:** HOLD_UNTIL_UNIQUE_RETURN_SCHEMA_CHECK

舊 Decision_Log／Linear／GitHub 固定 Return Target 與 QIN/BASE packet fields 已退休。Return target 與 representation 應由 Stable Existence、Domain、Authority、Purpose、Receiver 及 affected scope 決定。

## Retained primitives
- Exportable／Readable ≠ Approved／Validated。
- Projection/export 必須帶 Stable Identity、Source/Evidence pointer、Boundary、State、Claim Ceiling、Rights/Disclosure、Return Target。
- Return Written ≠ Receiver Reconciled；ACK/Absorption 由 Receiver 擁有。
- Public-safe ≠ Public-approved。
- Public projection 不暴露不必要的 private/native dependency body。

## Successor binding
`Effect/Projection → Evidence/Claim Boundary → Receiver Target → Return → Receiver-native Disposition → Reconciliation/Rebuild`

完整 packet predecessor 留 Git history；若 successor return semantics 已完整覆蓋且無 unique schema evidence，再進 pooled reclaim review。
