# 00 Mother-Law — Legacy Discovery Alias

**Lifecycle:** LEGACY_PATH_ALIAS  
**Current ontology role:** none  
**Normal reader:** no

此路徑只保留早期 Mother-Law／book-index／DXC-XDDL-XLEN／LingLuo 等 generation 的 bounded lineage。它不是 DCP Native Home、中央母法、第一層架構或必經入口。

## Current rule
有用能力只能以 primitive／evidence／failure／lineage 被 successor 吸收；舊名稱與路徑不得重新取得 Current effect。

`Historical root ≠ authority root`  
`Mother-law label ≠ current law`  
`Folder placement ≠ ontology`

正常 Reader 走 `CURRENT-SURFACE-MANIFEST.json` → `LIFECYCLE_DEPENDENCY_CHAIN_KERNEL.md`。本路徑只在 conflict／audit／missing lineage／failure reconstruction／explicit re-entry 時 bounded read。
